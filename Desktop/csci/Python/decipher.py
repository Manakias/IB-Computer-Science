import math
while 1==1:
    input = raw_input("DECODE: ")
    input = input.lower()
    output = []
    for character in input:
       number = ord(character) + 82
       b = 1.414
       alpha = 
       beta = int(alpha)
       uncipher = chr(beta)
       output.append(uncipher)
    print output
