import math
input=raw_input("DECODE: ")
input=input.lower()
output=""
array=[]
x=math.sqrt(572)
for integer in input:
    array = array+[integer]
    psi=2.53265
    chi=math.sqrt(integer
    G=chi**2
    upsilon=(((integer**-3)/x)+(beta/x))
    for i in range(len(array)):
        psi=psi/1.4532
    uncode=chr(integer)
    output.append(uncode)
print output
